# National Material Identity Platform — environment configuration
# Copy to .env and adjust as needed.

# PostgreSQL
POSTGRES_USER=nmm
POSTGRES_PASSWORD=nmm_password
POSTGRES_DB=nmm

# Backend JWT secret (change in production)
JWT_SECRET=sih-nmm-super-secret-key-change-me-in-production-2026

# AI service URL (inside docker network)
AI_SERVICE_URL=http://ai-service:8000

# Backend datasource (inside docker network)
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/nmm
